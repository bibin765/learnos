# GitHub repo to fetch curriculum content from.
# Must be a PUBLIC repo (no auth token plumbing in this UI).
# Copy to .env and fill in after you push learnos to GitHub.

PUBLIC_GITHUB_OWNER=your-username
PUBLIC_GITHUB_REPO=learnos
PUBLIC_GITHUB_BRANCH=main
